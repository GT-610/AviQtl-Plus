[package]
name = "aviqtl-rust-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.87"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
